import st from "../pages/Home.module.scss";

const Home = () => {
  return (
    <div className={st.wrapper}>
      <h1 className={st.title}>Hello from Homepage</h1>
    </div>
  );
};

export default Home;
